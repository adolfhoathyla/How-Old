language: objective-c
osx_image: xcode9.1
os: osx

xcode_workspace: HowOld.xcworkspace
xcode_scheme: HowOldTests

script:
  - xcodebuild test -workspace HowOld.xcworkspace -scheme 'HowOld' -sdk iphonesimulator build